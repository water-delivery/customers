const {
  setup,
  // teardown
} = require('./utils');

before(setup);

// after(teardown);

// describe('Initialize test setup', () => {
//   beforeEach(setup);

//   after(teardown);
//   it('Check if all tables are created', () => {
//   });
// });

